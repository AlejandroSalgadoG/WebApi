[project]
name = "token_auth"
version = "0.1.0"
description = "Api with token auth"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.7",
    "django-stubs>=5.2.7",
    "djangorestframework>=3.16.1",
    "mypy>=1.18.2",
]
